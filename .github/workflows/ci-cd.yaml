name: build-and-test
on:
  push:
    branches:
      - '**'
jobs:
  build-and-test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pip install -r requirements.txt
      - run: python findFirstIndexOfSubstringInaString.py
  
