name: build-and-test
on:
  push:
    branches:
      - '**'
jobs:
  build-and-test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo pip install -r requiremetns.txt
      - run: python findFirstIndexOfSubstringInaString.py
  
